packages:
  - 'packages/*'
  - 'apps/*'
allowBuilds:
  esbuild: true
  unrs-resolver: true
  # lerna dependency; its postinstall only primes the nx daemon/cache,
  # lerna works without it — keep the script blocked
  nx: false
linkWorkspacePackages: true
managePackageManagerVersions: false
minimumReleaseAge: 4320
trustPolicy: no-downgrade
trustPolicyIgnoreAfter: 43200
blockExoticSubdeps: true
overrides:
  cheerio: 1.0.0-rc.5
  css-what: ^5.0.1
  fast-uri: ^3.1.5 # fixes https://github.com/gr4vy/gr4vy-embed/security/dependabot/266
  sockjs>uuid: ^14.0.1
  # lerna/nx pin these transitively to exact vulnerable versions, so there is
  # no in-range upgrade available — drop each override once lerna ships a bump
  lerna>tar: ^7.5.22
  lerna>js-yaml: ^4.3.0
  ip-address: ^10.4.0
  nx>axios: ^1.18.0
  nx>brace-expansion: ^5.0.9
  postcss: ^8.5.23
